# 构建产物（可重建）
code/app/.build/
code/app/build/

# 运行时数据与日志（本机自用数据，不进版本库）
code/app/diag.csv
code/app/diag-*.csv
code/app/status.log
*.log

.DS_Store

# 内部文档不进公开仓库：设计决策、踩坑记录、产品定义、交接说明、给 AI 的项目指令。
# 这些文件仍然留在本地工作区（CLAUDE.md 必须在根目录才对 Claude Code 生效），只是不对外发布。
docs/
HANDOFF.md
CLAUDE.md

# 工具残留（Playwright MCP 截图、Python 字节码）
.playwright-mcp/
__pycache__/

# 本机 Claude Code 权限配置（因人而异，不进版本库）
.claude/settings.local.json

# 文章配套的 mp4（公众号上传用）不进仓库；README 用的 GIF 在 assets/ 下正常跟踪
